sudo: false
language: php
os:
  - linux
php:
  - 7.4
  - nightly
jobs:
  allow_failures:
  - php: nightly
before_script:
  - make build
  - make install
script: composer run-script test
notifications:
  email: false
